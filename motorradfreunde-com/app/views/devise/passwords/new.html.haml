- title "Du hast Dein Passwort vergessen?"
- hide_social_media_buttons

= form_for resource, as: resource_name, url: password_path(resource_name), html: { method: :post, role: :form } do |f|
  .form-group.row
    = f.label :email, class: 'col-sm-3 col-form-label'
    .col-sm-9
      = f.text_field :email, required: true, placeholder: true, class: 'form-control', autofocus: true
      %p.form-text.text-muted
        Trage deine eMail-Adresse ein, mit der du bei Motorradfreunde.com registriert bist.
        Du bekommst dann in wenigen Minuten eine eMail, in der beschrieben ist, wie du ein neues Passwort erstellen kannst.
  .actions
    = f.submit "Passwort senden", class: 'btn btn-primary'

= render("links")
